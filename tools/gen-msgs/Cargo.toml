[workspace]

[package]
name = "robot-bus-gen-msgs"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
heck = "0.5.0"
prost-build = { version = "0.14", features = ["cleanup-markdown"] }
