"""``python -m robot_bus`` is the SDK package, not the broker.

Start a standalone broker with::

    python -m robot_bus.broker
"""

from __future__ import annotations

raise SystemExit(
    "robot_bus is the SDK package. Start the broker with: python -m robot_bus.broker [options]"
)
