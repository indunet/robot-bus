syntax = "proto3";

package sensor_msgs.msg.v1;

import "std_msgs/msg/v1/header.proto";

// Equivalent to ROS 2 `sensor_msgs/msg/CompressedImage`.
message CompressedImage {
  std_msgs.msg.v1.Header header = 1;
  string format = 2;
  bytes data = 3;
}
