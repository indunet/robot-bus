syntax = "proto3";

package robot_bus_interfaces.srv.v1;

// 复位 — empty request; response reports whether reset succeeded.
message ResetRequest {}

message ResetResponse {
  bool success = 1;
  // Human-readable detail; typically set on failure.
  string msg = 2;
}
