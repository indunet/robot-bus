syntax = "proto3";

package lifecycle_msgs.srv.v1;

import "lifecycle_msgs/msg/v1/state.proto";

// Equivalent to ROS 2 `lifecycle_msgs/srv/GetState`.
message GetStateRequest {}

message GetStateResponse {
  lifecycle_msgs.msg.v1.State current_state = 1;
}
