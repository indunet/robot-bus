syntax = "proto3";

package tf2_msgs.msg.v1;

import "geometry_msgs/msg/v1/stamped.proto";

// Equivalent to ROS 2 `tf2_msgs/msg/TFMessage`.
message TFMessage {
  repeated geometry_msgs.msg.v1.TransformStamped transforms = 1;
}
