syntax = "proto3";

package geometry_msgs.v1;

// Equivalent to ROS 2 `geometry_msgs/msg/Point`.
// Position of a point in free space.
message Point {
  double x = 1;
  double y = 2;
  double z = 3;
}
