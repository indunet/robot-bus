syntax = "proto3";

package geometry_msgs.msg.v1;

// Equivalent to ROS 2 `geometry_msgs/msg/Quaternion`.
// Orientation in free space in quaternion form.
// ROS defaults: x=0, y=0, z=0, w=1.
message Quaternion {
  double x = 1;
  double y = 2;
  double z = 3;
  double w = 4;
}
