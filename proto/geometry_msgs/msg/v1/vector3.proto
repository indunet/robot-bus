syntax = "proto3";

package geometry_msgs.msg.v1;

// Equivalent to ROS 2 `geometry_msgs/msg/Vector3`.
// Vector in free space, semantically anchored at the origin.
message Vector3 {
  double x = 1;
  double y = 2;
  double z = 3;
}
